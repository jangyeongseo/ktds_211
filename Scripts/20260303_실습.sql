INSERT
	INTO
	BOARD
(ID,
TITLE,
CONTENT,
WRITE_DATE)
VALUES('BO-' || TO_CHAR(SYSDATE, 'YYYYMMDD-') || LPAD(SEQ_BOARD_PK.NEXTVAL, 6, 0), '', '', SYSDATE);

UPDATE
	TABLE_NAME
SET
	수정할 컬럼 = 수정할 값,
	수정할 컬럼 = 수정할 값,
	수정할 컬럼 = 수정할 값
WHERE
	수정대상 PK = PK값
;

UPDATE
	BOARD
SET
	TITLE = '',
	CONTENT = '',
	LATEST_MODIFY_DATE = SYSDATE
WHERE
	ID = 'BO-20260303-000004'
;

DELETE
FROM
	TABLE_NAME
WHERE
	삭제대상 PK = PK값
;

DELETE FROM BOARD
WHERE ID='';


-- 조회
UPDATE BOARD
SET VIEW_COUNT = VIEW_COUNT + 1
WHERE ID = 'BO-20260303-000016'
;

SELECT
	ID,
	TITLE,
	CONTENT,
	VIEW_COUNT,
	TO_CHAR(WRITE_DATE, 'YYYY-MM-DD HH24:MI:SS') AS WRITE_DATE,
	TO_CHAR(LATEST_MODIFY_DATE, 'YYYY-MM-DD HH24:MI:SS') AS LATEST_MODIFY_DATE
FROM
	BOARD
WHERE
	ID = 'BO-20260303-000016'
;

SELECT * FROM BOARD;
ROLLBACK;-- 변경사항 취소
COMMIT; -- 변경값을 확정